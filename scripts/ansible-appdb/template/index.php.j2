
<?php
$servername = "192.168.0.113";
$username = "ansible";
$password = "ansible";

// Create connection
$conn = mysqli_connect($servername, $username, $password);

// Check connection
if (!$conn) {
  die("Connection failed: " . mysqli_connect_error());
}
echo "Connected successfully to database from {{ ansible_fqdn }}";
?>